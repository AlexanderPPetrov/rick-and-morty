query GetCharacters($page: Int!) {
  characters(page: $page) {
    info {
      pages
      count
    }
    results {
      id
      name
      status
      species
      type
      gender
      image
      episode {
        id
        characters {
          id
        }
      }
    }
  }
}