query GetCharacters($page: Int!) {
  characters(page: $page) {
    info {
      pages
      count
    }
    results {
      ...CharacterBase
      location {
        ...LocationBase
      }
    }
  }
}